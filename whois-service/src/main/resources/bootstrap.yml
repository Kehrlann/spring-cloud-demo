---
---
spring.profiles: cloud
whois.delay.factor: 0.1
eureka:
  instance:
    nonSecurePort: 80
    hostname: ${vcap.application.uris[0]}
    metadata-map:
      management.port: 80
  client.service-url.defaultZone: ${vcap.services.eureka.credentials.url}

spring.zipkin.base-url: ${vcap.services.zipkin.credentials.url}
